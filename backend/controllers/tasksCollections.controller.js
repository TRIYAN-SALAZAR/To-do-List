const tasksCollectionsSchema = require('../schemas/tasksCollections');

const control = {};
control.addTaskToCollection = async (req, res) => {
    // tasksCollectionsSchema.updateOne({ title: 'simple' }, { $push: { tasks: req.body }})
}


module.exports = control;